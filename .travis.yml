language: node_js
cache:
  directories:
    - node_modules
notifications:
  email: false
install:
  - npm install
script:
  - npm run lint
  - make dist
after_success:
  - npm run semantic-release
branches:
  except:
    - /^v\d+\.\d+\.\d+$/
matrix:
  include:
    - node_js: 'node'
    - node_js: '6'
